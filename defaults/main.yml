---
# defaults variable file
lotus_user: lotus

system_dependencies:
  - gcc
  - git
  - bzr
  - jq
  - pkg-config
  - mesa-opencl-icd
  - ocl-icd-opencl-dev

install_dir: /opt/lotus

go_url: https://dl.google.com/go/go1.13.5.src.tar.gz
go_install_dir: /usr/local

git_url: https://github.com/filecoin-project/lotus.git

lotus_config_file: config.toml
config: {}

extra_run_args: []

perform_uninstall: false

custom_unit_properties: {}
